[project]
name = "outbound-call-gateway"
version = "0.1.0"
description = "LiveKit SIP <-> Ultravox bridge + SQS trigger worker"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["lk_ultravox_bridge"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
